   #include <stdio.h>
   #include <string.h>
   #include <stdlib.h>
   int main()
   {
       char *str;
       string = (char *)malloc(sizeof(char)*10);
       strcpy(str,"More than 10 bytes");
       printf("%s",str);
       return 0;
   }